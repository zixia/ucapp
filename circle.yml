machine:
  xcode:
    version: "7.0"
  services:
    - docker
  environment:

dependencies:
  post:

test:
  override:
    - echo circleci rocks!

notify:
  webhooks:
    url: http://zixia.net/~zixia/git/pushme.php
